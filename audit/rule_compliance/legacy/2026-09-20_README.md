# Historical 2026-09-20 preparation README — superseded

This is a dated description of the earlier package. Its missing-Top-1 statement and commands/paths describe that earlier layout, not the integrated revision 17 materials. Use the [current README](../README.md) for executable commands and included modules. Historical path tokens below are retained for provenance and are not promises of current local files.

**Scope:** archived 120-run study; focused criterion re-adjudication dated 2026-09-20. No new model executions. No GitHub upload, release, DOI or author sign-off is implied.

## Read in this order
1. `protocol/CRITERIA_CONTEXT_V1.md`: definition, boundaries, coverage.
2. `data/adjudication/decisions.csv`: 19 initial-role cases and 3 parent-turn cases with reasons.
3. `results/criteria_comparison.csv`: legacy, clear corrections, conservative boundaries.
4. `results/conservative_common_1320.csv`: conservative candidate; unchanged rows inherit prior judgments.
5. `AI_AND_HUMAN_REVIEW.md`, `limitations_and_coverage.csv`: evidence limits.

## Reproduce offline (Python standard library)
```
python code/reproduce.py --mode tables --out reproduced
python code/recheck_criteria.py --out reproduced_criteria
python -m unittest discover -s tests -v
python code/reproduce.py --mode manifest --out manifest_check
```

The first command reproduces the historical frozen-label results, NOT the amended table. The second applies the explicit patches and creates all three comparisons. Never cite a legacy output as the amended result. No API key or model call is needed. Optional PDF source checks use `requirements-source-checks.txt` and the historical archive; see `legacy/CLOSEOUT_README.md` for that command's original limited scope.

## Folder meanings
- `data/core`: immutable original 1,387-row ledger (1,320 common plus 67 selected).
- `data/adjudication`: new decisions, patch list and 120-window screening coverage.
- `data/supplemental_partial`: retained partial audits and coverage; not newly exhaustively adjudicated.
- `data/diagnostic_only`: extraction candidates; excluded from compliance rates.
- `protocol`, `annotations`: source rules and fixed prior coding/calculation inputs.
- `evidence/criteria_recheck`: bounded source text and selected inspected PDF images.
- `code`, `tests`: patched replay and amendment calculations.
- `results`, `qa`: current executable results and actual test logs.
- `legacy`: superseded closeout descriptions/results, preserved as dated history, not current status.

Original PDFs are already referenced at archive commit `6867755772bfb74dd56c211df8c9a399607b5419`. The commit identifies original records, not this new package or historical per-session attachments. Their hashes cannot prove actual past model inputs. Source paths in frozen historical ledgers are archival provenance, not execution dependencies.

## Interpret results
Do not pool heterogeneous audit units, treat 1,320 judgments as independent runs, or call 120 screened windows 120 fully revalidated roles. Eight ambiguous cases are not automatically errors; the conservative candidate withholds them. No full-AHP, human-equivalence, educational-effect or relative-model-performance claim is certified.

## Publication gate
Resolve and document author decisions on boundary coding; link actual human review records and evaluator provenance at their available evidentiary level; check rights to excerpts and inherited material. The public payload contains no reviewer correspondence, current manuscript, private Drive inventory, API secrets, survey participant identities, or blank human forms. No new blanket license is granted. Top-1 outcome recoding is not recomputed by this package and is not certified here. Update the parent repository README to distinguish this amendment from its legacy workbook analysis.
